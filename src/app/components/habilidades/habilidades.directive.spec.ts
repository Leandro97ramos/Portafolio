import { HabilidadesDirective } from './habilidades.directive';

describe('HabilidadesDirective', () => {
  it('should create an instance', () => {
    const directive = new HabilidadesDirective();
    expect(directive).toBeTruthy();
  });
});
